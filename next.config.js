/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = module.exports = {
    images: {
      remotePatterns: [
        {
          protocol: 'https',
          hostname: 'fakestoreapi.com',
        },
      ],
    },
  }
